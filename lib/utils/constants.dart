import 'package:flutter/material.dart';

const defaultVerticalSizedBox = SizedBox(height: 16.0);
const smallVerticalSizedBox = SizedBox(height: 8.0);

const largeHorizontalSizedBox = SizedBox(width: 16.0);
const defaultHorizontalSizedBox = SizedBox(width: 8.0);

const defaultPadding = EdgeInsets.all(16.0);

const BorderRadius defaultBorder = BorderRadius.all(Radius.circular(10.0));
